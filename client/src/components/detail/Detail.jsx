import { useSelector } from "react-redux";
import './detail.css';

const Detail = ()=>{

    const { countries } = useSelector((state)=>state);

    return(
        <div>

        </div>
    )
};

export default Detail;